version: "3"

services:
  mysql:
    image: mysql
    container_name: graal-vm-mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: db
    ports:
      - "3306:3306"
